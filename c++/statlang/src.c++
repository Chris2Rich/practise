enum Token {
    tok_eof = -1,

    tok_function = -2,
    tok_async = -3,
    tok_parallel = -4,
    tok_identifier = -5,

    tok_int = -6,
    tok_float = -7,
    tok_ivector = -8,
    tok_fvector = -9,
    tok_imatrix = -10,
    tok_fmatrix = -11,
};

int main(){
    return 0;
}